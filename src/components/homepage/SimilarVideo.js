import React from "react";

const SimilarVideo = ({ title }) => {
	return (
		<div className="h-40 bg-blue-200 p-2" style={{ maxWidth: "370px" }}>
			{title}
		</div>
	);
};

export default SimilarVideo;
